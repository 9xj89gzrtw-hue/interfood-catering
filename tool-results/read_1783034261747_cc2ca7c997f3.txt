     1→     1→#!/usr/bin/env bash
     2→     2→# ═══════════════════════════════════════════════════════════════════════
     3→     3→# QUALITY PIPELINE v3 — Interfood Catering Website
     4→     4→# Постоянный инженерный конвейер контроля качества
     5→     5→# v3.0 — 2026-07-03
     6→     6→#
     7→     7→# АВТОМАТИЧЕСКИ запускается перед каждым commit.
     8→     8→# Если хотя бы один CRITICAL этап не пройден:
     9→     9→#   - НЕ повышать версию
    10→    10→#   - НЕ обновлять MEMORY.md
    11→    11→#   - НЕ выполнять deploy
    12→    12→#   - НЕ сообщать пользователю что задача завершена
    13→    13→#
    14→    14→# Этапы:
    15→    15→#   P1 — Статический анализ (build, lint, types, dead code, deps)
    16→    16→#   P2 — Анализ кода (неиспользуемый код, дубли, опасные паттерны)
    17→    17→#   P3 — Проверка ресурсов (медиа, маршруты, ссылки)
    18→    18→#   P4 — Браузерное тестирование (agent-browser: интерактив, формы, mobile)
    19→    19→#   P5 — Визуальная регрессия (скриншоты до/после, сравнение)
    20→    20→#   P6 — Производительность (Lighthouse metrics)
    21→    21→#   P7 — Доступность (a11y через agent-browser)
    22→    22→#   P8 — Верификация багов (BUG_REGISTRY check)
    23→    23→# ═══════════════════════════════════════════════════════════════════════
    24→    24→
    25→    25→set -euo pipefail
    26→    26→
    27→    27→PROJECT_DIR="/home/z/my-project"
    28→    28→SITE_URL="${1:-https://interfood-catering.vercel.app}"
    29→    29→PIPELINE_DIR="$PROJECT_DIR/.pipeline"
    30→    30→REPORT="$PIPELINE_DIR/report-$(date +%Y%m%d-%H%M%S).md"
    31→    31→SCREENSHOT_BASE="$PIPELINE_DIR/screenshots"
    32→    32→SNAP_DIR="$PIPELINE_DIR/snapshots"
    33→    33→
    34→    34→# Counters
    35→    35→PASSED=0; FAILED=0; CRITICAL=0; SKIPPED=0; TOTAL=0
    36→    36→# Track which stages passed for summary
    37→    37→STAGES_PASSED=""; STAGES_FAILED=""
    38→    38→
    39→    39→mkdir -p "$PIPELINE_DIR" "$SCREENSHOT_BASE" "$SNAP_DIR"
    40→    40→
    41→    41→# ─── Helpers ───────────────────────────────────────────────────────
    42→    42→timestamp() { date '+%H:%M:%S'; }
    43→    43→
    44→    44→gate() {
    45→    45→  local stage="$1" name="$2" result="$3" evidence="${4:-}"
    46→    46→  local critical="${5:-false}"
    47→    47→  TOTAL=$((TOTAL + 1))
    48→    48→  local icon="✅"; local word="PASS"
    49→    49→  if [ "$result" = "FAIL" ]; then
    50→    50→    icon="❌"; word="FAIL"; FAILED=$((FAILED + 1))
    51→    51→    [ "$critical" = "true" ] && CRITICAL=$((CRITICAL + 1))
    52→    52→    STAGES_FAILED="$STAGES_FAILED $stage/$name"
    53→    53→  elif [ "$result" = "SKIP" ]; then
    54→    54→    icon="⏭️"; word="SKIP"; SKIPPED=$((SKIPPED + 1))
    55→    55→  else
    56→    56→    PASSED=$((PASSED + 1))
    57→    57→    STAGES_PASSED="$STAGES_PASSED $stage/$name"
    58→    58→  fi
    59→    59→  echo "  $icon [$stage] $name — $word ${evidence:+│ $evidence}"
    60→    60→  echo "$icon [$stage] $name — $word ${evidence:+│ $evidence}" >> "$REPORT"
    61→    61→  [ -n "$evidence" ] && echo "    → $evidence" >> "$REPORT"
    62→    62→}
    63→    63→
    64→    64→section() {
    65→    65→  echo ""
    66→    66→  echo "━━━ $1 ━━━"
    67→    67→  echo "" >> "$REPORT"
    68→    68→  echo "## $1" >> "$REPORT"
    69→    69→  echo "" >> "$REPORT"
    70→    70→}
    71→    71→
    72→    72→# ─── Initialize Report ─────────────────────────────────────────────
    73→    73→echo "# QUALITY PIPELINE v3 — $(date)" > "$REPORT"
    74→    74→echo "Site: $SITE_URL" >> "$REPORT"
    75→    75→echo "Project: $PROJECT_DIR" >> "$REPORT"
    76→    76→echo "" >> "$REPORT"
    77→    77→
    78→    78→echo ""
    79→    79→echo "═══════════════════════════════════════════════════════════"
    80→    80→echo "  QUALITY PIPELINE v3 — $(timestamp)"
    81→    81→echo "  Site: $SITE_URL"
    82→    82→echo "═══════════════════════════════════════════════════════════"
    83→    83→
    84→    84→# ═══════════════════════════════════════════════════════════════════
    85→    85→# P1: СТАТИЧЕСКИЙ АНАЛИЗ
    86→    86→# ═══════════════════════════════════════════════════════════════════
    87→    87→section "P1: СТАТИЧЕСКИЙ АНАЛИЗ"
    88→    88→cd "$PROJECT_DIR"
    89→    89→
    90→    90→# P1.1: Build
    91→    91→echo "── P1.1: Build ──"
    92→    92→BUILD_OUT=$(npm run build 2>&1)
    93→    93→if echo "$BUILD_OUT" | grep -q "✓ Generating static pages"; then
    94→    94→  PAGES=$(echo "$BUILD_OUT" | grep -c "○" || true)
    95→    95→  gate P1 "Build succeeds" PASS "Pages: $PAGES"
    96→    96→else
    97→    97→  gate P1 "Build succeeds" FAIL "Build failed" "true"
    98→    98→  echo '```' >> "$REPORT"; echo "$BUILD_OUT" | tail -20 >> "$REPORT"; echo '```' >> "$REPORT"
    99→    99→fi
   100→   100→
   101→   101→# P1.2: ignoreBuildErrors check
   102→   102→echo "── P1.2: ignoreBuildErrors ──"
   103→   103→if grep -q "ignoreBuildErrors.*true" next.config.ts 2>/dev/null; then
   104→   104→  gate P1 "ignoreBuildErrors disabled" FAIL "ignoreBuildErrors: true found — masks TS errors!" "true"
   105→   105→else
   106→   106→  gate P1 "ignoreBuildErrors disabled" PASS "Not present in config"
   107→   107→fi
   108→   108→
   109→   109→# P1.3: TypeScript
   110→   110→echo "── P1.3: TypeScript ──"
   111→   111→TSC_OUT=$(npx tsc --noEmit 2>&1 || true)
   112→   112→TSC_ERR=$(echo "$TSC_OUT" | grep "error TS" | grep -v "backups/" | grep -v "node_modules/" | grep -v "skills/" | grep -v "examples/" | grep -v ".next/" | wc -l || true)
   113→   113→if [ "$TSC_ERR" -eq 0 ]; then
   114→   114→  gate P1 "TypeScript clean" PASS "0 errors"
   115→   115→else
   116→   116→  gate P1 "TypeScript clean" FAIL "$TSC_ERR errors" "true"
   117→   117→  echo "$TSC_OUT" | grep "error TS" | grep -v "backups/" | head -10 >> "$REPORT"
   118→   118→fi
   119→   119→
   120→   120→# P1.4: ESLint
   121→   121→echo "── P1.4: ESLint ──"
   122→   122→LINT_OUT=$(npx eslint "src/**/*.{ts,tsx}" --max-warnings 9999 2>&1 || true)
   123→   123→LINT_ERR=$(echo "$LINT_OUT" | grep -c "error" || true)
   124→   124→LINT_WARN=$(echo "$LINT_OUT" | grep -c "warning" || true)
   125→   125→if [ "$LINT_ERR" -eq 0 ]; then
   126→   126→  gate P1 "ESLint 0 errors" PASS "Warnings: $LINT_WARN"
   127→   127→else
   128→   128→  gate P1 "ESLint 0 errors" FAIL "Errors: $LINT_ERR, Warnings: $LINT_WARN"
   129→   129→  echo "$LINT_OUT" | grep "error" | head -10 >> "$REPORT"
   130→   130→fi
   131→   131→
   132→   132→# P1.5: Unused dependencies
   133→   133→echo "── P1.5: Unused Dependencies ──"
   134→   134→DEPCHECK_OUT=$(npx depcheck --json 2>/dev/null || echo '{"dependencies":[],"devDependencies":[]}')
   135→   135→UNUSED_DEPS=$(echo "$DEPCHECK_OUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d.get('dependencies',[])))" 2>/dev/null || echo "unknown")
   136→   136→if [ "$UNUSED_DEPS" = "0" ]; then
   137→   137→  gate P1 "Unused dependencies" PASS "0 unused"
   138→   138→elif [ "$UNUSED_DEPS" = "unknown" ]; then
   139→   139→  gate P1 "Unused dependencies" SKIP "depcheck analysis unavailable"
   140→   140→else
   141→   141→  gate P1 "Unused dependencies" FAIL "$UNUSED_DEPS unused packages" "false"
   142→   142→  echo "$DEPCHECK_OUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print('\n'.join(d.get('dependencies',[])))" 2>/dev/null | head -10 >> "$REPORT"
   143→   143→fi
   144→   144→
   145→   145→# ═══════════════════════════════════════════════════════════════════
   146→   146→# P2: АНАЛИЗ КОДА
   147→   147→# ═══════════════════════════════════════════════════════════════════
   148→   148→section "P2: АНАЛИЗ КОДА"
   149→   149→
   150→   150→# P2.1: Dangerous patterns — pointer-events:none on interactive elements
   151→   151→echo "── P2.1: Pointer-events Safety ──"
   152→   152→BAD_POINTER=$(grep -rn "pointer-events.*none" src/components/SiteNav.tsx src/components/MenuBuilder.tsx src/components/sections/CTASection.tsx src/components/sections/ContactShowcase.tsx 2>/dev/null | grep -v "overlay\|grain\|trail\|cursor" | wc -l || true)
   153→   153→if [ "$BAD_POINTER" -eq 0 ]; then
   154→   154→  gate P2 "Pointer-events safe" PASS "No pointer-events:none on interactive"
   155→   155→else
   156→   156→  gate P2 "Pointer-events safe" FAIL "$BAD_POINTER suspicious pointer-events:none" "true"
   157→   157→fi
   158→   158→
   159→   159→# P2.2: z-index conflicts check
   160→   160→echo "── P2.2: z-index Audit ──"
   161→   161→ZINDEX_HIGH=$(grep -rn "z-\[9999\]\|z-\[9998\]\|z-\[999\]\|zIndex.*9999\|z-index.*9999" src/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "backups" | wc -l || true)
   162→   162→ZINDEX_MID=$(grep -rn "z-\[50\]\|z-\[40\]\|z-\[30\]\|z-50\|z-40\|z-30" src/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "backups" | wc -l || true)
   163→   163→gate P2 "z-index audit" PASS "High(z≥999): $ZINDEX_HIGH, Mid(z=30-50): $ZINDEX_MID — review for conflicts"
   164→   164→
   165→   165→# P2.3: Duplicate CSS properties
   166→   166→echo "── P2.3: Duplicate CSS Properties ──"
   167→   167→DUP_CSS=$(grep -rn "display.*display\|color.*color\|background.*background" src/components/ --include="*.tsx" 2>/dev/null | grep -v "backups" | grep "style=" | wc -l || true)
   168→   168→if [ "$DUP_CSS" -le 3 ]; then
   169→   169→  gate P2 "Duplicate CSS properties" PASS "$DUP_CSS potential duplicates (< 3)"
   170→   170→else
   171→   171→  gate P2 "Duplicate CSS properties" FAIL "$DUP_CSS potential duplicates" "false"
   172→   172→fi
   173→   173→
   174→   174→# P2.4: Console.log left in production code
   175→   175→echo "── P2.4: Console.log Audit ──"
   176→   176→CONSOLE_LOGS=$(grep -rn "console\.log\|console\.warn\|console\.error\|console\.debug" src/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "backups" | grep -v "node_modules" | wc -l || true)
   177→   177→if [ "$CONSOLE_LOGS" -le 10 ]; then
   178→   178→  gate P2 "Console statements" PASS "$CONSOLE_LOGS console calls (≤10 acceptable)"
   179→   179→else
   180→   180→  gate P2 "Console statements" FAIL "$CONSOLE_LOGS console calls (should clean up)" "false"
   181→   181→fi
   182→   182→
   183→   183→# ═══════════════════════════════════════════════════════════════════
   184→   184→# P3: ПРОВЕРКА РЕСУРСОВ
   185→   185→# ═══════════════════════════════════════════════════════════════════
   186→   186→section "P3: ПРОВЕРКА РЕСУРСОВ"
   187→   187→
   188→   188→# P3.1: Broken image references
   189→   189→echo "── P3.1: Image References ──"
   190→   190→BROKEN_IMGS=0
   191→   191→for img_ref in $(grep -roh '/images/[^"'\''`) ]*\.\(jpg\|png\|webp\|svg\)' src/ 2>/dev/null | sort -u); do
   192→   192→  if [ ! -f "$PROJECT_DIR/public$img_ref" ]; then
   193→   193→    echo "  BROKEN: $img_ref" >> "$REPORT"
   194→   194→    BROKEN_IMGS=$((BROKEN_IMGS + 1))
   195→   195→  fi
   196→   196→done
   197→   197→if [ "$BROKEN_IMGS" -eq 0 ]; then
   198→   198→  gate P3 "Image references valid" PASS "0 broken"
   199→   199→else
   200→   200→  gate P3 "Image references valid" FAIL "$BROKEN_IMGS broken image refs" "true"
   201→   201→fi
   202→   202→
   203→   203→# P3.2: Video references
   204→   204→echo "── P3.2: Video References ──"
   205→   205→BROKEN_VIDS=0
   206→   206→for vid_ref in $(grep -roh '/videos/[^"'\''`) ]*\.\(mp4\|webm\)' src/ 2>/dev/null | sort -u); do
   207→   207→  if [ ! -f "$PROJECT_DIR/public$vid_ref" ]; then
   208→   208→    echo "  BROKEN VIDEO: $vid_ref" >> "$REPORT"
   209→   209→    BROKEN_VIDS=$((BROKEN_VIDS + 1))
   210→   210→  fi
   211→   211→done
   212→   212→if [ "$BROKEN_VIDS" -eq 0 ]; then
   213→   213→  gate P3 "Video references valid" PASS "0 broken"
   214→   214→else
   215→   215→  gate P3 "Video references valid" FAIL "$BROKEN_VIDS broken video refs" "true"
   216→   216→fi
   217→   217→
   218→   218→# P3.3: Route availability (HTTP check)
   219→   219→echo "── P3.3: Route HTTP Status ──"
   220→   220→ROUTES="/ /menu /services /about /calculator /reviews /contacts /wedding /corporate /gallery /faq /quiz /blog /venues /team /privacy /terms"
   221→   221→ROUTE_FAILS=""
   222→   222→for route in $ROUTES; do
   223→   223→  CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$SITE_URL$route" 2>/dev/null || echo "000")
   224→   224→  if [ "$CODE" != "200" ]; then
   225→   225→    ROUTE_FAILS="$ROUTE_FAILS $route→$CODE"
   226→   226→    echo "  FAIL: $route → $CODE" >> "$REPORT"
   227→   227→  fi
   228→   228→done
   229→   229→if [ -z "$ROUTE_FAILS" ]; then
   230→   230→  gate P3 "All routes HTTP 200" PASS "17 routes checked"
   231→   231→else
   232→   232→  gate P3 "All routes HTTP 200" FAIL "Failed:$ROUTE_FAILS" "true"
   233→   233→fi
   234→   234→
   235→   235→# P3.4: Internal link consistency
   236→   236→echo "── P3.4: Internal Links ──"
   237→   237→LINK_TARGETS=$(grep -roh 'href="/[^"]*"' src/ 2>/dev/null | sort -u | wc -l || true)
   238→   238→gate P3 "Internal links cataloged" PASS "$LINK_TARGETS unique href targets in source"
   239→   239→
   240→   240→# ═══════════════════════════════════════════════════════════════════
   241→   241→# P4: БРАУЗЕРНОЕ ТЕСТИРОВАНИЕ (agent-browser)
   242→   242→# ═══════════════════════════════════════════════════════════════════
   243→   243→section "P4: БРАУЗЕРНОЕ ТЕСТИРОВАНИЕ"
   244→   244→
   245→   245→# P4.1: Homepage loads, no console errors
   246→   246→echo "── P4.1: Homepage Console ──"
   247→   247→agent-browser open "$SITE_URL" --timeout 15000 2>&1 | head -3
   248→   248→sleep 4
   249→   249→ERRORS_RAW=$(agent-browser errors 2>&1 || true)
   250→   250→ERROR_COUNT=$(echo "$ERRORS_RAW" | grep -c "✗" || true)
   251→   251→if [ "$ERROR_COUNT" -lt 10 ]; then
   252→   252→  gate P4 "Console errors < 10" PASS "Errors: $ERROR_COUNT"
   253→   253→else
   254→   254→  gate P4 "Console errors < 10" FAIL "Errors: $ERROR_COUNT" "true"
   255→   255→fi
   256→   256→
   257→   257→# P4.2: Hero visible
   258→   258→echo "── P4.2: Hero Content ──"
   259→   259→HERO_TEXT=$(agent-browser eval "document.querySelector('h1')?.textContent?.trim()?.substring(0, 80)" 2>&1 || echo "")
   260→   260→if echo "$HERO_TEXT" | grep -qi "интерфуд"; then
   261→   261→  gate P4 "Hero heading visible" PASS "Text: '${HERO_TEXT:0:50}'"
   262→   262→else
   263→   263→  gate P4 "Hero heading visible" FAIL "Got: '${HERO_TEXT:0:50}'" "true"
   264→   264→fi
   265→   265→
   266→   266→# P4.3: Navigation — all main links present and clickable
   267→   267→echo "── P4.3: Navigation Links ──"
   268→   268→NAV_SNAP=$(agent-browser snapshot -i -s "nav" 2>&1 || echo "")
   269→   269→NAV_ITEMS="КОНСТРУКТОР МЕНЮ|УСЛУГИ|О НАС|КАЛЬКУЛЯТОР|ОТЗЫВЫ|КОНТАКТЫ"
   270→   270→MISSING_NAV=""
   271→   271→for item in $(echo "$NAV_ITEMS" | tr '|' ' '); do
   272→   272→  if ! echo "$NAV_SNAP" | grep -q "$item"; then
   273→   273→    MISSING_NAV="$MISSING_NAV $item"
   274→   274→  fi
   275→   275→done
   276→   276→if [ -z "$MISSING_NAV" ]; then
   277→   277→  gate P4 "Navigation complete" PASS "All 6 nav items present"
   278→   278→else
   279→   279→  gate P4 "Navigation complete" FAIL "Missing:$MISSING_NAV" "true"
   280→   280→fi
   281→   281→
   282→   282→# P4.4: CTA buttons clickable
   283→   283→echo "── P4.4: CTA Buttons ──"
   284→   284→CTA_CLICKS=0; CTA_COVERED=0
   285→   285→CTA_REFS=$(agent-browser snapshot -i 2>&1 | grep -i "Рассчитать\|Расчёт\|ЗАКАЗАТЬ\|ОСТАВИТЬ" | grep -oP '@e\d+' | head -5 || true)
   286→   286→for ref in $CTA_REFS; do
   287→   287→  CLICK_RESULT=$(agent-browser click "$ref" 2>&1 || echo "FAIL")
   288→   288→  if echo "$CLICK_RESULT" | grep -q "covered by"; then
   289→   289→    CTA_COVERED=$((CTA_COVERED + 1))
   290→   290→    echo "  COVERED: $ref" >> "$REPORT"
   291→   291→  else
   292→   292→    CTA_CLICKS=$((CTA_CLICKS + 1))
   293→   293→  fi
   294→   294→done
   295→   295→if [ "$CTA_COVERED" -eq 0 ]; then
   296→   296→  gate P4 "CTA buttons clickable" PASS "$CTA_CLICKS clicked, 0 covered"
   297→   297→else
   298→   298→  gate P4 "CTA buttons clickable" FAIL "$CTA_COVERED CTA buttons covered by other elements!" "true"
   299→   299→fi
   300→   300→
   301→   301→# P4.5: MenuBuilder interactive test
   302→   302→echo "── P4.5: MenuBuilder ──"
   303→   303→agent-browser eval "document.getElementById('menu-builder')?.scrollIntoView({behavior:'instant'}); 'ok'" 2>&1 >/dev/null
   304→   304→sleep 3
   305→   305→MB_FULL=$(agent-browser snapshot -i 2>&1 || echo "")
   306→   306→MB_ADD_REF=$(echo "$MB_FULL" | grep "Добавить" | head -1 | grep -oP '@e\d+' || echo "")
   307→   307→if [ -n "$MB_ADD_REF" ]; then
   308→   308→  MB_CLICK=$(agent-browser click "$MB_ADD_REF" 2>&1 || echo "FAIL")
   309→   309→  if echo "$MB_CLICK" | grep -q "covered by"; then
   310→   310→    gate P4 "MenuBuilder add clickable" FAIL "Add button COVERED by element!" "true"
   311→   311→  else
   312→   312→    sleep 1
   313→   313→    MB_AFTER=$(agent-browser snapshot -i 2>&1 || echo "")
   314→   314→    if echo "$MB_AFTER" | grep -q "Убрать из меню\|Уменьшить"; then
   315→   315→      gate P4 "MenuBuilder add works" PASS "Item added, controls appeared"
   316→   316→    else
   317→   317→      gate P4 "MenuBuilder add works" FAIL "Clicked but no cart update" "true"
   318→   318→    fi
   319→   319→  fi
   320→   320→else
   321→   321→  # Fallback: try scrolling more
   322→   322→  agent-browser scroll down 5000 2>&1 || true; sleep 2
   323→   323→  MB_FULL2=$(agent-browser snapshot -i 2>&1 || echo "")
   324→   324→  MB_ADD_REF2=$(echo "$MB_FULL2" | grep "Добавить" | head -1 | grep -oP '@e\d+' || echo "")
   325→   325→  if [ -n "$MB_ADD_REF2" ]; then
   326→   326→    gate P4 "MenuBuilder found (scroll)" PASS "Found after scroll"
   327→   327→  else
   328→   328→    gate P4 "MenuBuilder found" FAIL "No 'Добавить' button found" "true"
   329→   329→  fi
   330→   330→fi
   331→   331→
   332→   332→# P4.6: Mobile viewport testing (375x812)
   333→   333→echo "── P4.6: Mobile (375×812) ──"
   334→   334→agent-browser set viewport 375 812 2>&1 || true
   335→   335→agent-browser reload 2>&1 || true
   336→   336→sleep 4
   337→   337→MOBILE_HERO=$(agent-browser eval "document.querySelector('h1')?.textContent?.trim()?.substring(0,50)" 2>&1 || echo "")
   338→   338→if echo "$MOBILE_HERO" | grep -qi "интерфуд"; then
   339→   339→  gate P4 "Mobile hero visible" PASS "Text: '${MOBILE_HERO:0:40}'"
   340→   340→else
   341→   341→  gate P4 "Mobile hero visible" FAIL "Got: '${MOBILE_HERO:0:40}'" "true"
   342→   342→fi
   343→   343→
   344→   344→# P4.7: Mobile MenuBuilder
   345→   345→echo "── P4.7: Mobile MenuBuilder ──"
   346→   346→agent-browser eval "document.getElementById('menu-builder')?.scrollIntoView({behavior:'instant'}); 'ok'" 2>&1 >/dev/null
   347→   347→sleep 3
   348→   348→MOBILE_MB=$(agent-browser snapshot -i 2>&1 || echo "")
   349→   349→MOBILE_ADD=$(echo "$MOBILE_MB" | grep "Добавить" | head -1 | grep -oP '@e\d+' || echo "")
   350→   350→if [ -n "$MOBILE_ADD" ]; then
   351→   351→  MOBILE_CLICK=$(agent-browser click "$MOBILE_ADD" 2>&1 || echo "FAIL")
   352→   352→  if echo "$MOBILE_CLICK" | grep -q "covered by"; then
   353→   353→    gate P4 "Mobile MenuBuilder clickable" FAIL "Button COVERED on mobile!" "true"
   354→   354→  else
   355→   355→    gate P4 "Mobile MenuBuilder clickable" PASS "Button works on mobile"
   356→   356→  fi
   357→   357→else
   358→   358→  gate P4 "Mobile MenuBuilder found" FAIL "No add button on mobile" "true"
   359→   359→fi
   360→   360→
   361→   361→# P4.8: Mobile menu open/close
   362→   362→echo "── P4.8: Mobile Menu Toggle ──"
   363→   363→# Scroll to top first
   364→   364→agent-browser eval "window.scrollTo(0,0); 'ok'" 2>&1 >/dev/null; sleep 1
   365→   365→MOBILE_SNAP=$(agent-browser snapshot -i 2>&1 || echo "")
   366→   366→BURGER_REF=$(echo "$MOBILE_SNAP" | grep -i "Открыть меню\|меню\|burger" | head -1 | grep -oP '@e\d+' || echo "")
   367→   367→if [ -n "$BURGER_REF" ]; then
   368→   368→  BURGER_CLICK=$(agent-browser click "$BURGER_REF" 2>&1 || echo "FAIL")
   369→   369→  if echo "$BURGER_CLICK" | grep -q "covered by"; then
   370→   370→    gate P4 "Mobile menu button" FAIL "Burger button covered!" "true"
   371→   371→  else
   372→   372→    sleep 1
   373→   373→    # Try to close
   374→   374→    CLOSE_REF=$(agent-browser snapshot -i 2>&1 | grep -i "Закрыть" | head -1 | grep -oP '@e\d+' || echo "")
   375→   375→    if [ -n "$CLOSE_REF" ]; then
   376→   376→      CLOSE_CLICK=$(agent-browser click "$CLOSE_REF" 2>&1 || echo "FAIL")
   377→   377→      if echo "$CLOSE_CLICK" | grep -q "covered by"; then
   378→   378→        gate P4 "Mobile menu close" FAIL "Close button covered on mobile!" "true"
   379→   379→      else
   380→   380→        gate P4 "Mobile menu toggle" PASS "Open and close work"
   381→   381→      fi
   382→   382→    else
   383→   383→      # Try Escape to close
   384→   384→      agent-browser press Escape 2>&1 || true
   385→   385→      gate P4 "Mobile menu open" PASS "Opened (closed via Escape)"
   386→   386→    fi
   387→   387→  fi
   388→   388→else
   389→   389→  gate P4 "Mobile menu button" FAIL "No burger button found" "true"
   390→   390→fi
   391→   391→
   392→   392→# P4.9: Footer visible
   393→   393→echo "── P4.9: Footer ──"
   394→   394→agent-browser eval "window.scrollTo(0, document.body.scrollHeight); 'ok'" 2>&1 >/dev/null
   395→   395→sleep 1
   396→   396→FOOTER_TEXT=$(agent-browser eval "document.querySelector('footer')?.textContent?.substring(0,100)" 2>&1 || echo "")
   397→   397→if echo "$FOOTER_TEXT" | grep -qi "интерфуд\|Интерфуд"; then
   398→   398→  gate P4 "Footer visible on mobile" PASS "Content found"
   399→   399→else
   400→   400→  gate P4 "Footer visible on mobile" FAIL "No footer content" "true"
   401→   401→fi
   402→   402→
   403→   403→# ═══════════════════════════════════════════════════════════════════
   404→   404→# P5: ВИЗУАЛЬНАЯ РЕГРЕССИЯ (скриншоты)
   405→   405→# ═══════════════════════════════════════════════════════════════════
   406→   406→section "P5: ВИЗУАЛЬНАЯ РЕГРЕССИЯ"
   407→   407→
   408→   408→# P5.1: Desktop screenshots
   409→   409→echo "── P5.1: Desktop Screenshots ──"
   410→   410→agent-browser set viewport 1920 1080 2>&1 || true
   411→   411→agent-browser reload 2>&1 || true
   412→   412→sleep 4
   413→   413→DESK_HOME="$SCREENSHOT_BASE/desktop-home-$(date +%Y%m%d-%H%M%S).png"
   414→   414→agent-browser screenshot "$DESK_HOME" 2>&1 || true
   415→   415→
   416→   416→# Scroll to MenuBuilder
   417→   417→agent-browser eval "document.getElementById('menu-builder')?.scrollIntoView({behavior:'instant'}); 'ok'" 2>&1 >/dev/null
   418→   418→sleep 2
   419→   419→DESK_MB="$SCREENSHOT_BASE/desktop-menubuilder-$(date +%Y%m%d-%H%M%S).png"
   420→   420→agent-browser screenshot "$DESK_MB" 2>&1 || true
   421→   421→
   422→   422→# P5.2: Mobile screenshots
   423→   423→echo "── P5.2: Mobile Screenshots ──"
   424→   424→agent-browser set viewport 375 812 2>&1 || true
   425→   425→agent-browser reload 2>&1 || true
   426→   426→sleep 4
   427→   427→MOB_HOME="$SCREENSHOT_BASE/mobile-home-$(date +%Y%m%d-%H%M%S).png"
   428→   428→agent-browser screenshot "$MOB_HOME" 2>&1 || true
   429→   429→
   430→   430→# P5.3: Tablet screenshots
   431→   431→echo "── P5.3: Tablet Screenshots ──"
   432→   432→agent-browser set viewport 768 1024 2>&1 || true
   433→   433→agent-browser reload 2>&1 || true
   434→   434→sleep 4
   435→   435→TAB_HOME="$SCREENSHOT_BASE/tablet-home-$(date +%Y%m%d-%H%M%S).png"
   436→   436→agent-browser screenshot "$TAB_HOME" 2>&1 || true
   437→   437→
   438→   438→# P5.4: Visual diff (compare with previous snapshot if exists)
   439→   439→echo "── P5.4: Visual Diff ──"
   440→   440→LATEST_SNAP=$(ls -t "$SNAP_DIR"/desktop-home-*.png 2>/dev/null | head -1 || echo "")
   441→   441→if [ -n "$LATEST_SNAP" ] && [ -f "$LATEST_SNAP" ] && [ -f "$DESK_HOME" ]; then
   442→   442→  # Save current as new snapshot
   443→   443→  cp "$DESK_HOME" "$SNAP_DIR/desktop-home-$(date +%Y%m%d-%H%M%S).png"
   444→   444→  # Compare file sizes as a rough proxy (pixel comparison needs extra tools)
   445→   445→  OLD_SIZE=$(stat -f%z "$LATEST_SNAP" 2>/dev/null || stat -c%s "$LATEST_SNAP" 2>/dev/null || echo 0)
   446→   446→  NEW_SIZE=$(stat -f%z "$DESK_HOME" 2>/dev/null || stat -c%s "$DESK_HOME" 2>/dev/null || echo 0)
   447→   447→  SIZE_DIFF=$((NEW_SIZE - OLD_SIZE))
   448→   448→  gate P5 "Visual snapshot comparison" PASS "Size diff: ${SIZE_DIFF} bytes (previous: ${OLD_SIZE}, current: ${NEW_SIZE})"
   449→   449→  echo "  NOTE: Pixel-level comparison not available — using file size as proxy" >> "$REPORT"
   450→   450→  echo "  Install Percy/Chromatic for real visual regression testing" >> "$REPORT"
   451→   451→else
   452→   452→  # First run — save as baseline
   453→   453→  [ -f "$DESK_HOME" ] && cp "$DESK_HOME" "$SNAP_DIR/desktop-home-$(date +%Y%m%d-%H%M%S).png"
   454→   454→  gate P5 "Visual baseline captured" PASS "First run — baseline saved for future comparison"
   455→   455→fi
   456→   456→
   457→   457→# ═══════════════════════════════════════════════════════════════════
   458→   458→# P6: ПРОИЗВОДИТЕЛЬНОСТЬ
   459→   459→# ═══════════════════════════════════════════════════════════════════
   460→   460→section "P6: ПРОИЗВОДИТЕЛЬНОСТЬ"
   461→   461→
   462→   462→# P6.1: Lighthouse (via agent-browser)
   463→   463→echo "── P6.1: Performance Metrics ──"
   464→   464→agent-browser set viewport 1920 1080 2>&1 || true
   465→   465→agent-browser reload 2>&1 || true
   466→   466→sleep 3
   467→   467→
   468→   468→# Use Navigation Timing API for basic metrics
   469→   469→PERF_DATA=$(agent-browser eval "JSON.stringify({
   470→   470→  loadTime: Math.round(performance.timing.loadEventEnd - performance.timing.navigationStart),
   471→   471→  domReady: Math.round(performance.timing.domContentLoadedEventEnd - performance.timing.navigationStart),
   472→   472→  resources: performance.getEntriesByType('resource').length,
   473→   473→  transferSize: performance.getEntriesByType('resource').reduce((a,r)=>a+(r.transferSize||0),0)
   474→   474→})" 2>&1 || echo "{}")
   475→   475→
   476→   476→LOAD_TIME=$(echo "$PERF_DATA" | python3 -c "import sys,json; d=json.loads(sys.stdin.read().strip() or '{}'); print(d.get('loadTime','?'))" 2>/dev/null || echo "?")
   477→   477→DOM_READY=$(echo "$PERF_DATA" | python3 -c "import sys,json; d=json.loads(sys.stdin.read().strip() or '{}'); print(d.get('domReady','?'))" 2>/dev/null || echo "?")
   478→   478→RESOURCES=$(echo "$PERF_DATA" | python3 -c "import sys,json; d=json.loads(sys.stdin.read().strip() or '{}'); print(d.get('resources','?'))" 2>/dev/null || echo "?")
   479→   479→TRANSFER=$(echo "$PERF_DATA" | python3 -c "import sys,json; d=json.loads(sys.stdin.read().strip() or '{}'); print(d.get('transferSize','?'))" 2>/dev/null || echo "?")
   480→   480→
   481→   481→if [ "$LOAD_TIME" != "?" ] && [ "$LOAD_TIME" -lt 10000 ]; then
   482→   482→  gate P6 "Page load time" PASS "Load: ${LOAD_TIME}ms, DOM: ${DOM_READY}ms, Resources: ${RESOURCES}, Transfer: ${TRANSFER}B"
   483→   483→elif [ "$LOAD_TIME" != "?" ] && [ "$LOAD_TIME" -ge 10000 ]; then
   484→   484→  gate P6 "Page load time" FAIL "Load: ${LOAD_TIME}ms (>10s!)" "true"
   485→   485→else
   486→   486→  gate P6 "Page load time" SKIP "Could not measure"
   487→   487→fi
   488→   488→
   489→   489→# P6.2: Largest Contentful Paint
   490→   490→echo "── P6.2: Core Web Vitals ──"
   491→   491→LCP_DATA=$(agent-browser eval "JSON.stringify(new Promise(r=>{new PerformanceObserver((list)=>{const entries=list.getEntries();r(entries[entries.length-1]?.startTime||0)}).observe({type:'largest-contentful-paint',buffered:true});setTimeout(()=>r(-1),3000)}))" 2>&1 || echo "-1")
   492→   492→gate P6 "LCP measured" SKIP "LCP requires async observer — value: $LCP_DATA (use Lighthouse CI for reliable metrics)"
   493→   493→
   494→   494→# ═══════════════════════════════════════════════════════════════════
   495→   495→# P7: ДОСТУПНОСТЬ
   496→   496→# ═══════════════════════════════════════════════════════════════════
   497→   497→section "P7: ДОСТУПНОСТЬ"
   498→   498→
   499→   499→# P7.1: ARIA landmarks
   500→   500→echo "── P7.1: ARIA Landmarks ──"
   501→   501→LANDMARKS=$(agent-browser eval "document.querySelectorAll('[role], nav, main, footer, header, aside, section').length" 2>&1 || echo "0")
   502→   502→if [ "$LANDMARKS" -ge 5 ]; then
   503→   503→  gate P7 "ARIA landmarks present" PASS "$LANDMARKS landmarks found"
   504→   504→else
   505→   505→  gate P7 "ARIA landmarks present" FAIL "Only $LANDMARKS landmarks (< 5)" "false"
   506→   506→fi
   507→   507→
   508→   508→# P7.2: Image alt text
   509→   509→echo "── P7.2: Image Alt Text ──"
   510→   510→NO_ALT=$(agent-browser eval "Array.from(document.querySelectorAll('img:not([alt])')).length" 2>&1 || echo "?")
   511→   511→if [ "$NO_ALT" = "0" ]; then
   512→   512→  gate P7 "Images have alt text" PASS "0 images without alt"
   513→   513→elif [ "$NO_ALT" != "?" ] && [ "$NO_ALT" -le 3 ]; then
   514→   514→  gate P7 "Images have alt text" FAIL "$NO_ALT images missing alt (minor)"
   515→   515→else
   516→   516→  gate P7 "Images have alt text" FAIL "$NO_ALT images missing alt!" "false"
   517→   517→fi
   518→   518→
   519→   519→# P7.3: Form labels
   520→   520→echo "── P7.3: Form Labels ──"
   521→   521→UNLABELED=$(agent-browser eval "Array.from(document.querySelectorAll('input:not([type=hidden]):not([type=submit])')).filter(i=>!i.labels?.length&&!i.getAttribute('aria-label')&&!i.getAttribute('aria-labelledby')).length" 2>&1 || echo "?")
   522→   522→if [ "$UNLABELED" = "0" ]; then
   523→   523→  gate P7 "Form inputs labeled" PASS "All inputs have labels"
   524→   524→else
   525→   525→  gate P7 "Form inputs labeled" FAIL "$UNLABELED inputs without labels" "false"
   526→   526→fi
   527→   527→
   528→   528→# P7.4: Color contrast (basic check)
   529→   529→echo "── P7.4: Color Contrast ──"
   530→   530→# This is a simplified check — real contrast checking needs axe-core or pa11y
   531→   531→gate P7 "Color contrast" SKIP "Automated pixel-level contrast check not available — use axe-core/pa11y CI. Design system uses #1A1714 on #FAFAF7 (>12:1 ratio) which passes WCAG AAA"
   532→   532→
   533→   533→# ═══════════════════════════════════════════════════════════════════
   534→   534→# P8: ВЕРИФИКАЦИЯ БАГОВ
   535→   535→# ═══════════════════════════════════════════════════════════════════
   536→   536→section "P8: ВЕРИФИКАЦИЯ БАГОВ"
   537→   537→
   538→   538→BUG_REGISTRY="$PROJECT_DIR/BUG_REGISTRY.md"
   539→   539→if [ -f "$BUG_REGISTRY" ]; then
   540→   540→  OPEN_BUGS=$(grep -c "❌ OPEN" "$BUG_REGISTRY" 2>/dev/null || echo "0")
   541→   541→  FIXED_BUGS=$(grep -c "✅ FIXED" "$BUG_REGISTRY" 2>/dev/null || echo "0")
   542→   542→  VERIFIED_BUGS=$(grep -c "✔ VERIFIED" "$BUG_REGISTRY" 2>/dev/null || echo "0")
   543→   543→  if [ "$OPEN_BUGS" -gt 0 ]; then
   544→   544→    gate P8 "Bug registry: no open bugs" FAIL "$OPEN_BUGS open bugs, $FIXED_BUGS fixed, $VERIFIED_BUGS verified"
   545→   545→  else
   546→   546→    gate P8 "Bug registry: no open bugs" PASS "All bugs resolved"
   547→   547→  fi
   548→   548→else
   549→   549→  gate P8 "Bug registry exists" FAIL "BUG_REGISTRY.md not found!" "true"
   550→   550→fi
   551→   551→
   552→   552→# ═══════════════════════════════════════════════════════════════════
   553→   553→# CLOSE BROWSER
   554→   554→# ═══════════════════════════════════════════════════════════════════
   555→   555→agent-browser close 2>&1 || true
   556→   556→
   557→   557→# ═══════════════════════════════════════════════════════════════════
   558→   558→# ИТОГОВЫЙ ОТЧЁТ
   559→   559→# ═══════════════════════════════════════════════════════════════════
   560→   560→echo "" >> "$REPORT"
   561→   561→echo "## ИТОГОВЫЙ ОТЧЁТ" >> "$REPORT"
   562→   562→echo "" >> "$REPORT"
   563→   563→echo "| Метрика | Значение |" >> "$REPORT"
   564→   564→echo "|---------|----------|" >> "$REPORT"
   565→   565→echo "| PASSED | $PASSED |" >> "$REPORT"
   566→   566→echo "| FAILED | $FAILED |" >> "$REPORT"
   567→   567→echo "| CRITICAL | $CRITICAL |" >> "$REPORT"
   568→   568→echo "| SKIPPED | $SKIPPED |" >> "$REPORT"
   569→   569→echo "| TOTAL | $TOTAL |" >> "$REPORT"
   570→   570→echo "" >> "$REPORT"
   571→   571→echo "### Пройденные этапы:" >> "$REPORT"
   572→   572→echo "$STAGES_PASSED" | tr ' ' '\n' | grep -v '^$' >> "$REPORT"
   573→   573→echo "" >> "$REPORT"
   574→   574→echo "### Проваленные этапы:" >> "$REPORT"
   575→   575→echo "$STAGES_FAILED" | tr ' ' '\n' | grep -v '^$' >> "$REPORT"
   576→   576→echo "" >> "$REPORT"
   577→   577→
   578→   578→echo ""
   579→   579→echo "═══════════════════════════════════════════════════════════"
   580→   580→echo "  PASSED: $PASSED  │  FAILED: $FAILED  │  CRITICAL: $CRITICAL  │  SKIPPED: $SKIPPED  │  TOTAL: $TOTAL"
   581→   581→echo "═══════════════════════════════════════════════════════════"
   582→   582→
   583→   583→if [ "$CRITICAL" -gt 0 ]; then
   584→   584→  echo ""
   585→   585→  echo "  🚫 PIPELINE FAILED — $CRITICAL критических ошибок!"
   586→   586→  echo "  ЗАПРЕЩЕНО: version bump, MEMORY update, deploy, task completion"
   587→   587→  echo "  Исправь критические ошибки и перезапусти pipeline."
   588→   588→  echo ""
   589→   589→  echo "Report: $REPORT"
   590→   590→  exit 1
   591→   591→elif [ "$FAILED" -gt 0 ]; then
   592→   592→  echo ""
   593→   593→  echo "  ⚠️  PIPELINE PASSED WITH WARNINGS — $FAILED некритических проблем"
   594→   594→  echo "  Commit разрешён, но рекомендуется исправить перед deploy."
   595→   595→  echo ""
   596→   596→  echo "Report: $REPORT"
   597→   597→  exit 0
   598→   598→else
   599→   599→  echo ""
   600→   600→  echo "  ✅ PIPELINE PASSED — все этапы пройдены"
   601→   601→  echo "  Разрешено: version bump, commit, push, deploy"
   602→   602→  echo ""
   603→   603→  echo "Report: $REPORT"
   604→   604→  exit 0
   605→   605→fi
   606→   606→